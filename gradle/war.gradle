apply plugin: "war"

bootWar {
    mainClassName = "org.projekt17.fahrschuleasa.FahrschuleAsaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    webAppDirName = "build/resources/main/static/"
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
